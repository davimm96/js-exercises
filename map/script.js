const cores = ["vermelho", "azul", "amarelo", "verde"]
cores.push("rosa", "roxo", "lilás")


const lista = cores.map(function(item){
    return alert(`${item}, possui ${item.length} caracteres`)
})

// cores.push(novasCores[n])



